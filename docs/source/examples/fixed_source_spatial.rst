Fixed Source from Existing Geometry
===================================

This example creates the spatial source directly from the already-built
material/region maps and loads the energy dependence from a separate file.

.. literalinclude:: ../../../examples/fixed_source_spatial.py
   :language: python
   :linenos:
